services:
    household-hq:
        container_name: "household-hq"
        build:
            context: './docker'
            dockerfile: Dockerfile
            args:
                WWWGROUP: "${WWWGROUP}"
        image: 'sail-8.4/app'
        extra_hosts:
            - 'host.docker.internal:host-gateway'
        environment:
            WWWUSER: "${WWWUSER}"
            LARAVEL_SAIL: 1
            XDEBUG_MODE: "${SAIL_XDEBUG_MODE:-off}"
            XDEBUG_CONFIG: "${SAIL_XDEBUG_CONFIG:-client_host=host.docker.internal}"
            IGNITION_LOCAL_SITES_PATH: "${PWD}"
        labels:
            - "traefik.enable=true"
            - "traefik.docker.network=docker_lan_network"
            - "traefik.http.routers.household.rule=Host(`household.docker.lan`)"
            - "traefik.http.routers.household.entrypoints=web"
            - "traefik.http.services.household.loadbalancer.server.port=80"
        volumes:
            - './laravel:/var/www/html'
            - './docker/supervisord.conf:/etc/supervisor/conf.d/supervisord.conf'
        networks:
            - docker_lan_network
        depends_on:
            - household-hq-db
            - household-hq-cache
        logging:
            driver: "json-file"
            options:
                max-size: "10m"
                max-file: "3"

    household-hq-db:
        container_name: "household-hq-db"
        image: 'mysql/mysql-server:8.0'
        environment:
            MYSQL_ROOT_PASSWORD: "${MYSQL_ROOT_PASSWORD}"
            MYSQL_ROOT_HOST: '%'
            MYSQL_DATABASE: "${MYSQL_DATABASE}"
            MYSQL_USER: "${MYSQL_USER}"
            MYSQL_PASSWORD: "${MYSQL_PASSWORD}"
            MYSQL_ALLOW_EMPTY_PASSWORD: 1
        volumes:
            - 'household-hq-db:/var/lib/mysql'
        networks:
            - docker_lan_network
        healthcheck:
            test:
                - CMD
                - mysqladmin
                - ping
                - "-p${MYSQL_PASSWORD}"
            retries: 3
            timeout: 5s

    household-hq-cache:
        container_name: "household-hq-cache"
        image: 'redis:alpine'
        volumes:
            - 'household-hq-cache:/data'
        networks:
            - docker_lan_network
        healthcheck:
            test:
                - CMD
                - redis-cli
                - ping
            retries: 3
            timeout: 5s

networks:
    docker_lan_network:
        external: true

volumes:
    household-hq-db:
        driver: local
    household-hq-cache:
        driver: local
